import React from 'react';
import { View, Text } from 'react-native';

const Fields = ({ fields }) => {
  console.log(fields);
  return (
    <View>
      <Text>Fields</Text>
    </View>
  )
};

export default Fields;
